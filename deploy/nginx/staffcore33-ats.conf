server {
    listen 80;
    server_name ats.staffcore33.com;  # change to your domain or VM IP

    client_max_body_size 30M;

    # Frontend (Next.js)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Backend API (Spring Boot)
    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# OPTIONAL HTTPS (after certbot):
# server {
#     listen 443 ssl http2;
#     server_name ats.staffcore33.com;
#     ssl_certificate     /etc/letsencrypt/live/ats.staffcore33.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ats.staffcore33.com/privkey.pem;
#     ... same location blocks as above ...
# }
