# Local overrides — do not commit secrets to public repos
# Used by docs; run-local.ps1 sets these in the process environment.
DB_USER=postgres
DB_PASSWORD=root

# Absolute or relative path for resume/uploads (run-local.ps1 uses project\device)
# FILE_UPLOAD_DIR=C:\Users\hp\staffcore33-ats\device
